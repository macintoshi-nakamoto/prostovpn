# Исходники движка тянутся скриптом Engine/build.sh
Engine/src/

# Собранный движок кладём в репозиторий: без него приложение не соберётся
# на машине без Go. Всё остальное — мусор сборки.
build/
dist/
*.xcuserdatad
xcuserdata/
DerivedData/
.DS_Store

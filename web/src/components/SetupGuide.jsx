import { GuideBody } from "./GuideBody.jsx";
import "./setup-guide.css";

/**
 * Инструкция во вкладке кабинета.
 *
 * Показывает ровно ту же инструкцию, что и страница /guide: пять платформ,
 * живые ссылки на установщики, скриншоты и раздел про привычные сервисы.
 * Раньше здесь был свой сокращённый текст на четыре платформы — он отставал
 * от публичной страницы, и человек в кабинете получал инструкцию хуже, чем
 * гость с лендинга.
 *
 * Логин передаётся дальше: в боковой карточке он подставляется в строку
 * «чем входить», чтобы не заставлять человека вспоминать его самому.
 */
export function SetupGuide({ login }) {
  return (
    <div className="sg">
      <GuideBody login={login} embedded />
    </div>
  );
}
